import React, { FC } from "react";
import { FolderLayout, FolderShow } from "./components";

export const Folder: FC = () => {
  return (
    <FolderLayout>
      <FolderShow />
    </FolderLayout>
  );
};
